#!/usr/bin/python3
def print_list_integer(my_list=[]):
    for num in range(len(my_list)):
        print("{:d}".format(my_list[num]))


"""#!/usr/bin/python3
def print_list_integer(my_list=[]):
    str = "{}"
    for itm in my_list:
        print(str.format(itm))"""
